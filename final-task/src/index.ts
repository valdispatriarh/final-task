// import App from './components/app/app';
// import './global.css';

// const app = new App();
// console.log('1');

export default function sum(a: number, b: number, c: number, d: number) {
    return a + b + c + d;
}
